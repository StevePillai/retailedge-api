services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development
      - DB_HOST=db
    depends_on: [db, cache]

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_PASSWORD: devpass

  cache:
    image: redis:7-alpine
